# 更新日志

## 2026-08-28

### API Key 默认值安全检查

#### 问题

用户担心下载项目后会继承仓库中的默认 API Key。

#### 原因

项目没有把 API Key 写入源码或设置默认值，但旧的设置提示不够明确；使用过项目的浏览器可能会在 `sessionStorage` 中保留该浏览器此前填写的 Key。

#### 修复

- 确认仓库源码、服务端和测试文件中没有真实 API Key。
- API Key 继续只从当前浏览器 `sessionStorage` 读取，无 Key 时返回空字符串。
- 设置弹窗明确提示首次使用必须填写用户自己的 API Key。
- API Key 输入框标记为 `new-password`，避免浏览器密码管理器自动填入旧凭据。
- 新用户使用全新浏览器会话下载运行时，API Key 输入框为空。

## 2026-08-28

### 修复视频生成 `num_frames` 参数错误

#### 问题

部分视频任务生成失败，Agnes 返回以下错误：

```text
Upstream API request failed. { "status": 400, "response": { "code": "fail to fetch task", "message": "num_frames 151 is invalid; num_frames must equal 8 * n + 1" } }
```

#### 原因

Agnes 视频接口要求 `num_frames` 必须符合 `8n + 1`，可用值包括 `1、9、17、25、33...`。旧版本在读取旧节点数据、手动帧数或特定时长与帧率组合时，可能直接提交 `151` 等非法值。

内置的 4、5、6、8 秒选项本身对应 `97、121、145、193` 帧，均符合接口要求；问题来自异常或历史帧数没有在请求前统一校验。

#### 修复

- 前端统一规范化视频节点的帧数，非法值会自动调整为最近的合法 `8n + 1` 值，例如 `151` 调整为 `153`。
- 修改视频时长、手动帧数、读取旧画布数据和生成视频时，都会执行同一套校验。
- 服务端 Agnes 代理增加二次校验，避免旧页面或其他客户端继续提交非法帧数。
- 帧数输入框在修改后会显示最终实际使用的合法值。
- 更新前端脚本缓存版本，确保浏览器加载修复后的代码。

#### 验证

- `4s @ 24fps -> 97 frames`
- `5s @ 24fps -> 121 frames`
- `6s @ 24fps -> 145 frames`
- `8s @ 24fps -> 193 frames`
- `151 -> 153`
- `npm run check` 通过
- `npm test` 全部通过
